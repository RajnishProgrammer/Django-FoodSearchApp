"""
you'll required this when django < 3.2, i have upper version of django but for learning i declared it, 
it will load signal so the user profile will be auto-created.
"""
default_app_config = 'users.apps.UsersConfig'